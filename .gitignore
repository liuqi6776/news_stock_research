__pycache__/
*.py[cod]
*$py.class
.env
.ipynb_checkpoints/
output.txt
v5_signals_result.txt

# Large data files (>100MB, exceed GitHub limit)
research/**/data/*.parquet
research/**/predictions/*.parquet

# Global binary and cache ignores
*.parquet
*.pkl
*.joblib
*.zip
*.log
*.mp4
*.mp3
*.avi
*.mov

# Generated data files (should not be in git)
*.csv
!/data/.gitkeep
!/etf-valuation-strategy/data/.gitkeep
!/research/**/data/.gitkeep
daily_chip.csv

# 抓取新闻网页数据（运行时生成，不应进 git）
jiayo-analysis/data/

# Agent session memory (private, not for repo)
memory/

# Nested repo git backup (moved from .tmp_qc/.git, keep local only)
.tmp_qc/.git_backup/

# Conclusion archive repo (nested independent git, keep local only)
quant_conclusion/

# Sensitive authentication QR codes and screenshots (keep local only)
lark*.png
qr*.png
*auth_qr*.png
*qrcode*.png